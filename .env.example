# Docker Compose settings for the local PostgreSQL instance
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=booktracker

# Prisma and build settings for database access
DATABASE_URL="postgresql://postgres:changeme@localhost:5433/booktracker?schema=public"

# NextAuth settings for local development
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="<generate me>"
AUTH_TRUST_HOST=true

# Maildev settings for the local SMTP inbox
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
