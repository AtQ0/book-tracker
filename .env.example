# Which email provider to use
# Options:
#   maildev   => local development with MailDev SMTP
#   sendgrid  => production on Render using SendGrid HTTP API
EMAIL_PROVIDER=maildev

# Database settings for the local PostgreSQL instance used with Docker Compose
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=booktracker

# Prisma and application settings for database access in local development
DATABASE_URL="postgresql://postgres:changeme@localhost:5433/booktracker?schema=public"

# NextAuth settings for local development
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="CHANGEME_generate_with: openssl rand -hex 32"
AUTH_TRUST_HOST=true
APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# SMTP settings for the Maildev inbox used in development
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
MAIL_FROM="no-reply@book-tracker.local"

# Optional settings for SendGrid (HTTP API) used in production
# To enable SendGrid in development for testing:
# 1. Set EMAIL_PROVIDER=sendgrid
# 2. Uncomment SENDGRID_API_KEY and MAIL_FROM below
#SENDGRID_API_KEY=SG.xxxxxxx
#MAIL_FROM="Book Tracker <mail@yourdomain.com>"
